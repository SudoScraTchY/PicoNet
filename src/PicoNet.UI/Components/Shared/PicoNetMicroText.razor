@implements IDisposable

<span class="tracking-[0.35em] uppercase text-muted">@_texts[_index]</span>

@code {
    private int _index;
    private Timer? _timer;
    private readonly string[] _texts = ["Half-Way There", "Shorter Journeys", "Link Less. Reach More.", "Direction Matters", "Every Click Counts"];

    protected override void OnInitialized() => _timer = new Timer(_ => { _index = (_index + 1) % _texts.Length; InvokeAsync(StateHasChanged); }, null, 3000, 3000);
    public void Dispose() => _timer?.Dispose();
}